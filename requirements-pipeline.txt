# Full-pipeline container requirements — union of the root requirements.txt
# (classification/retrieval/QA stages) and src/ingestion/requirements.txt
# (ingestion stage's document-parsing dependencies).
#
# Kept as a separate file rather than merged into requirements.txt so local
# dev (which may only need the lightweight stages) doesn't have to pull in
# torch/docling unless it's actually building the container.

# --- Core (all stages) ---
python-dotenv
litellm

# --- Hybrid retrieval (Stage 4/5) ---
rank_bm25
langdetect

# --- Postgres/pgvector storage ---
psycopg[binary]
pgvector

# --- Ingestion: document parsing ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
docling==2.114.0
pandas==2.2.3
tabulate==0.9.0
